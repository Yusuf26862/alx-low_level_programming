#include <stdio.h>
#include "main.h"
#include <stdlib.h>

/**
 * mul - multiply two value together
 *
 * @a: first to add to second.
 * @b: second int to add to first.
 *
 * Return: the sum of the result of c
 */
int mul(int a, int b);
int mul(int a, int b)
{
	int c = a * b;

	return (c);
}
