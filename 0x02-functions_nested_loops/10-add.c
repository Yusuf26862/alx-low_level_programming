#include <stdio.h>
#include <stdlib.h>

/**
 * add - adds two value together
 *
 * @a, b: The character to be checked.
 *
 * Return: the sum of the 2
 */
int _abs(int a, b);
int _abs(int a, b)
{
	c = a + b;
	return (c);
}
